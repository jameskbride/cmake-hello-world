#include <iostream>

#include "math/Max.h"
// #include <Speaker.h>

// using namespace std;
// using namespace Hello;

int main(int argc, char *argv[]) {
  // Speaker* speaker = new Speaker();
  // speaker->sayHello();  

  int maxValue = Max(10, 12);
	std::cout << "hello world :" << maxValue << std::endl;

  return 0;
}
